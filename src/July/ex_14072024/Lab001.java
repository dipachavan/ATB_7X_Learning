package ex_14072024;

public class Lab001 {
    public static void main(String[] args) {
        //Operators
        int age=30;
        //syntax
        //data_type variable_name(identifier)= variable_value(literals)
        //age->operand,= ->operator,30->value(unary plus)

        int aud=-20;
        //syntax
        //data_type variable_name(identifier)= variable_value(literals)
        //aug->operand,= ->operator,-20->value(unary minus)

        //Binary Operators-2 operands required,on which u can perform arithmetic operations(+,-,%,*,/)
        int a=10,b=30;
        int sum=a+b;
        //a and b -> 2 operands,sum -> operand,+ -> operator

        //Ternary Operators-3 operands
        //Compound Assignment Operators--> +=,-=,/=,*=
        //Relational Operators--> >,<,>=,<=.!,!=,==(compare two values)
        //Logical Operators--> OR or AND, ||,&&,!

        //new,instanceOf operator(OOPs concepts)
    }
}
